<h1 align='center'> <%= "#{@user.username}" %>'s Account </h1>

<div class='row'>
  <div class='col-xs-4 col-xs-offset-4 center'>
    <%= gravatar_for(@user, size: 100) %>
  </div>
</div>

<%=  render 'articles/article', obj: @user.articles %>